import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function main() { try { const user = await prisma.user.create({ data: { id: "goat", username: "gage", email: "gage.mccoy@gauntletai.com", imageUrl: "https://api.dicebear.com/7.x/avataaars/svg?seed=gage", isOnline: false, roleId: "1" }}); const channel = await prisma.channel.findFirst({ where: { name: "team-updates" }}); if (channel) { await prisma.channelMember.create({ data: { userId: "goat", channelId: channel.id }}); console.log("User created and added to channel successfully!"); } } catch (e) { console.error("Error:", e); } finally { await prisma.$disconnect(); } } main();
